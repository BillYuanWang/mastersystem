# MD Desk 旧版原型（已停用）

这里保存的是 2026 年 7 月早期的英文/CSV 课表原型源代码，仅供追溯，不再构建或运行。

- 唯一受支持的 macOS App 是项目根目录的 `MD Desk.app`。
- 旧的 `macos-app/MD Desk.app` 已删除。
- 旧构建脚本现在只会转到项目根目录的新版构建脚本，不会重新生成旧 App。
- `MD Desk Data/courses.csv` 暂时作为只读历史备份保留，不参与新版 Supabase 数据同步。
